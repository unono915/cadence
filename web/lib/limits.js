/**
 * 입력 길이의 상한 — 서버와 화면이 **같은 숫자**를 본다.
 *
 * 서버는 처음부터 길이를 검사했다. 문제는 화면이 그 숫자를 몰랐다는 것이다.
 * 그래서 카테고리 이름을 마흔 자 넘게 적고 저장을 누른 다음에야 빨간 오류를 봤다 —
 * 막을 수 있었던 실패를, 다 쓴 뒤에 알려 주는 셈이다. 한글 이름은 마흔 자가 생각보다
 * 금방 찬다.
 *
 * 두 곳에 숫자를 각각 적어 두면 언젠가 한쪽만 바뀐다. 그래서 한 군데서만 정하고
 * 양쪽이 그것을 가져다 쓴다. 이 파일이 `web/` 에 있는 것은 브라우저가 `server/` 를
 * 읽을 수 없기 때문이다 — 노드는 반대로 어느 쪽이든 읽을 수 있다.
 *
 * 화면의 `maxlength` 는 **붙여넣기까지** 막아 주지만, 그것이 검사를 대신하지는 않는다.
 * 서버는 화면을 거치지 않고 들어오는 요청도 받으므로 자기 검사를 그대로 유지한다.
 */
export const LIMITS = {
  /** 분류 규칙의 패턴. 정규식도 이 길이를 넘지 못한다(`safeRegex`). */
  RULE_PATTERN: 200,
  /** 카테고리 이름. 짧다 — 표와 태그 안에 들어가야 한다. */
  CATEGORY_NAME: 40,
  /** 태스크 제목. */
  TASK_TITLE: 300,
  /** 태스크 메모. 진행 상황을 이어 적는 칸이라 넉넉하다. */
  TASK_NOTES: 8000,
  /** 프로젝트 이름. */
  PROJECT_NAME: 120,
  /** 집중 세션에 붙이는 한 줄. */
  SESSION_NOTE: 1000,
  /** 빠른 메모 한 줄 — 오늘 노트 맨 아래에 덧붙는다. */
  NOTE_LINE: 2000,
  /** 활동 기록의 앱 이름(손으로 넣을 때). */
  ACTIVITY_APP: 120,
  /** 활동 기록의 창 제목. 추적기가 넣는 값도 여기서 잘린다. */
  ACTIVITY_TITLE: 400,
  /** 하루 노트 전체. 자동 저장되는 큰 칸이라 사실상 제한이 없다. */
  DAY_NOTE: 100_000,
};
