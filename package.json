{
  "name": "cadence",
  "version": "0.1.0",
  "private": true,
  "description": "Cadence — 로컬 우선(local-first) 업무 생산성 콘솔. 자동 활동 추적 + 태스크 + 집중 세션 + 분석.",
  "type": "module",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "start": "node server/index.mjs",
    "dev": "node --watch server/index.mjs",
    "test": "node --test \"test/**/*.test.mjs\"",
    "demo": "node scripts/seed-demo.mjs",
    "test:clock": "node scripts/clock-sweep.mjs"
  },
  "dependencies": {}
}
